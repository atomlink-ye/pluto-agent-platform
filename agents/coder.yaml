name: coder
description: Implements per the architecture spec; produces the artifact body.
model: test-model
system: |-
  You are the coder. Read the architect's spec, implement the requested
  artifact, return concise, runnable output.
