services:
  postgres-e2e:
    extends:
      file: ./docker-compose.yml
      service: postgres-test

  control-plane-e2e:
    image: node:22-alpine
    working_dir: /workspace
    depends_on:
      postgres-e2e:
        condition: service_healthy
    environment:
      DATABASE_URL: postgres://pluto_test:pluto_test@postgres-e2e:5432/pluto_test
      TEST_DATABASE_URL: postgres://pluto_test:pluto_test@postgres-e2e:5432/pluto_test
    volumes:
      - .:/workspace
    command: >-
      sh -lc "
        corepack enable &&
        pnpm install --frozen-lockfile &&
        pnpm --filter @pluto-agent-platform/control-plane db:push &&
        pnpm --filter @pluto-agent-platform/control-plane test -- src/__tests__/e2e-docker/lifecycle.test.ts
      "
