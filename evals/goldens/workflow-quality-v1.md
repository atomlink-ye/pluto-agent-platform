<!-- Deterministic reference artifact from the FakeAdapter workflow-quality-v1 run. -->

# Pluto MVP-alpha workflow quality reference run

Goal: Produce a markdown artifact that demonstrates team convergence. The artifact must clearly include the team lead synthesis plus planner, generator, and evaluator contributions, and it must not expose internal adapter prompt protocol text.

## Worker contributions

### planner
1. Outline the artifact.
2. Cover acceptance criteria.
3. Flag risks.

### generator
Generated body for instructions: Generate the artifact body for: Produce a markdown artifact that demonstrates team convergence. The artifact must clearly include the team lead synthesis plus planner, generator, and evaluator contributions, and it must not expose internal adapter prompt protocol text.

### evaluator
PASS: deliverable matches the team goal.

## Lead summary
Lead aggregated 3 worker outputs (planner, generator, evaluator).

## Evidence expectation

The golden run must also persist `evidence.md` and schema-valid `evidence.json` with no secret-shaped content.
